#include "main.h"

/**
* add - adds two integers.
*
* @a: input integer one.
* @b: input integer two.
*
* Return: returns result(a + b)
*/
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
